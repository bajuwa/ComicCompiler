Command:
> comcom
Expected Output:
- A series of pages that don't cut through bubbles/text but do cut on available whitespace between panels
- 3-4 pages of roughly equal height
Common Error Output:
- [Issue #TBD] image40.jpg appears to end in whitespace, but does not 'break' at that point (adding colour error does not resolve the issue)